<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <%= link_to 'ToDoList', root_url, class: 'navbar-brand'%>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <% if !user_signed_in?%>
                    <li class="nav-item">
                        <%= link_to 'Entrar', root_url, class: 'nav-link active'%>
                    </li>
                    <li class="nav-item">
                        <%= link_to 'Criar Conta', new_user_path, class: 'nav-link'%>
                    </li>
                <% else %>
                    <li class="nav-item inline dropdown">
                        <a class="nav-link dropdown-toggle active" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Bem vindo, <%= current_user.name %></a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <%= link_to 'Minhas listas', lists_path, class: 'dropdown-item' %>
                            <%= link_to 'Editar Conta', edit_user_path(current_user), class: 'dropdown-item' %>
                            <div class="dropdown-divider"></div>
                            <%= link_to 'Sair', sair_path, method: :delete, class: 'dropdown-item' %>
                        </div>
                    </li>
                    <li class="nav-item">
                        <%= link_to 'Listas', lists_path, class: 'nav-link active'%>
                    </li>
                <% end %>
            </ul>            
        </div>
    </div>
</nav>
